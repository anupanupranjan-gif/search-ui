# SearchX Frontend

React e-commerce search UI for the search-api backend.

## Setup

```bash
npm install
npm start
```

Make sure the search API is port-forwarded first:
```bash
kubectl port-forward service/search-api 8081:80
```

Then open http://localhost:3000
